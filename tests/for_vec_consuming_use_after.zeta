def consume(value: Box[Int]): Int = *value


def main(): Int = {
    var values: Vec[Box[Int]] = Vec[Box[Int]]()
    values.push(Box(1))

    for (value in values) do {
        consume(value)
    }

    values.length
}
