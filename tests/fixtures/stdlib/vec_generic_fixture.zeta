// Frontière générique interne validant Vec[T] dans une interface précompilée.
pub def consumeLength[T](values: Vec[T]): Int = values.length

pub def consumeIsEmpty[T](values: Vec[T]): Bool = values.isEmpty
