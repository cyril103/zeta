def incrementAll(values: SliceMut[Int]): Unit = {
    for (mut value in values) do {
        *value = *value + 1
    }
}


def main(): Int = {
    var values: [Int; 3] = [1, 2, 3]
    incrementAll(SliceMut(&mut values))
    if (values[0] == 2 && values[1] == 3 && values[2] == 4) 0 else 1
}
