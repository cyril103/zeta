import io

struct Packet {
    text: String
    code: Int
}


def main(): Int = {
    val suffix: String = "string"
    val packet: Packet = Packet { text: "heap " + suffix, code: 7 }
    io.println(packet.text)
    if (packet.text.lengthBytes == 11 && packet.code == 7) 0 else 1
}
