struct Point {
    x: Int
    y: Int
}

struct Pixel {
    position: Point
    visible: Bool
}

def main(): Int = {
    val point: Point = Point { x: 19, y: 23 }
    val pixel: Pixel = Pixel { position: point, visible: true }
    if (pixel.position.x + pixel.position.y == 42) { 0 } else { 1 }
}
