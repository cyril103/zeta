import vectors

def main(): Int = {
    var values: Vec[Int] = Vec[Int]()
    values.push(40)
    values.push(2)
    val length: Int = vectors.consumeLength(values)

    val empty: Vec[String] = Vec[String]()
    val isEmpty: Bool = vectors.consumeIsEmpty(empty)
    if (length == 2 && isEmpty) 0 else 1
}
